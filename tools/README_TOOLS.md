# Tools Toko Hana

- `make_cq_compiled.py` = generator build single-file CQ/HTML.

Script ini sudah dipatch untuk membaca asset aktif dari folder `assets/` dan source aktif dari root `App.jsx`, jadi build ulang tidak balik ke icon/logo lama.
